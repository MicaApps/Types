<Window
    x:Class="Types.WinUI.TypeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Types.WinUI"
    mc:Ignorable="d">
    <!--<Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisConverter" />
    </Window.Resources>-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header and Properties -->
        <StackPanel Grid.Row="0" Spacing="12" Padding="20">
            <TextBlock x:Name="ExtensionText" Style="{ThemeResource TitleTextBlockStyle}" />
            
            <TextBox x:Name="TitleBox" x:Uid="TypeWindow_Description" Header="Description" PlaceholderText="File Type Description" />
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="IconBox"
                         x:Uid="TypeWindow_DefaultIcon"
                         Grid.Column="0"
                         Header="Default Icon"
                         PlaceholderText="Path to icon file (e.g. shell32.dll,0)" />
                <Button Grid.Column="1"
                        x:Uid="BrowseButton"
                        Margin="8,24,0,0"
                        Content="Browse"
                        Click="BrowseIcon_Click"
                        />
            </Grid>
        </StackPanel>

        <!-- Actions (Verbs) -->
        <Grid Grid.Row="1" Padding="20,0,20,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
                <TextBlock x:Uid="TypeWindow_Actions" Text="Actions" Style="{ThemeResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
                <!-- Separator -->
                <Rectangle Width="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" VerticalAlignment="Stretch" Margin="4,4,4,4"/>
                <Button x:Uid="TypeWindow_AddAction" Content="Add Action" Click="AddVerb_Click" />
                <Button x:Uid="TypeWindow_Remove" Content="Remove" Click="RemoveVerb_Click" />
                <Button x:Uid="TypeWindow_SetDefault" Content="Set Default" Click="SetDefaultVerb_Click" />
            </StackPanel>
            
            <ListView Grid.Row="1" x:Name="VerbsList" SelectionMode="Single" BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" DoubleTapped="VerbsList_DoubleTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE790;" FontSize="12" Visibility="Visible"/>
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Command}" Opacity="0.6" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="10" Padding="20" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}">
            <Button x:Uid="SaveButton" Content="Save" Style="{ThemeResource AccentButtonStyle}" Click="SaveButton_Click" />
            <Button x:Uid="TypeWindow_Close" Content="Close" Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>
