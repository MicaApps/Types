<Window
    x:Class="Types.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Types.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Types">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / Search -->
        <Grid Grid.Row="0" Padding="12" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" x:Uid="SearchBox" PlaceholderText="Search extensions or types..." TextChanged="SearchBox_TextChanged" />
            <Button Grid.Column="1" Content="Edit Icon" x:Name="EditIconButton" x:Uid="EditIconButton" IsEnabled="False" Click="EditIcon_Click" />
            <Button Grid.Column="2" Content="Refresh" x:Name="RefreshButton" x:Uid="RefreshButton" Click="Refresh_Click" />
            <Button Grid.Column="3" Content="Settings" x:Name="SettingsButton" x:Uid="SettingsButton" Click="Settings_Click" />
        </Grid>

        <!-- List -->
        <ListView Grid.Row="1" x:Name="TypesList" SelectionMode="Single" DoubleTapped="TypesList_DoubleTapped" SelectionChanged="TypesList_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Icon placeholder -->
                        <Image Source="{Binding IconImage}" Width="16" Height="16" Grid.Column="0" VerticalAlignment="Center"/> 
                        
                        <TextBlock Text="{Binding Id}" Grid.Column="1" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Title}" Grid.Column="2" VerticalAlignment="Center" Opacity="0.8"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- Status Bar -->
        <TextBlock Grid.Row="2" x:Name="StatusText" Padding="12,4" FontSize="12" Opacity="0.6" />
    </Grid>
</Window>
